{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
    <div class="container pt-5">
        <h1 class="h2" style"font-size: 26px;line-height: 35px;padding: 20px 0;">Categories</h1>
        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ category.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ category.name }}</td>
                </tr>
                <tr>
                    <th>CategoryOrder</th>
                    <td>{{ category.categoryOrder }}</td>
                </tr>
                <tr>
                    <th>Slug</th>
                    <td>{{ category.Slug }}</td>
                </tr>
            </tbody>
    </table>
    <a href="{{ path('app_categories_index') }} " class="btn btn-primary mt-3">back to list</a><br>
    <a href="{{ path('app_categories_edit', {'id': category.id}) }}" class="btn btn-success mt-3">edit</a>
    {{ include('categories/_delete_form.html.twig') }}


    </div>
    

    

    


    
{% endblock %}
